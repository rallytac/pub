<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MapActivity">

    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:map="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MapActivity">

        <Button
            android:id="@+id/buttonPdList"
            android:layout_gravity="end|top"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginTop="70dp"
            android:layout_marginEnd="12dp"
            android:background="@drawable/ic_map_button_list"
            app:srcCompat="@drawable/ic_map_button_list"
            android:onClick="onClickShowPdList"
            android:visibility="visible"/>


        <Button
            android:id="@+id/buttonNextView"
            android:layout_gravity="end|top"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginTop="125dp"
            android:layout_marginEnd="12dp"
            android:background="@drawable/ic_layers"
            app:srcCompat="@drawable/ic_layers"
            android:onClick="onClickNextView"
            android:visibility="visible"/>
    </fragment>

</androidx.constraintlayout.widget.ConstraintLayout>
